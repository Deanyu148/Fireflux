# 安全策略

## 支持范围

只对**最新 Release**（当前 0.1.x）提供安全修复，旧版本请先升级再复现问题。

## 上报漏洞

请**不要**在公开 issue 里贴漏洞细节，改用 GitHub 的私密上报：

1. 打开本仓库的 **Security** 标签页
2. 点 **Report a vulnerability**（中文界面为「报告漏洞」）
3. 写清楚复现步骤、影响范围，以及客户端版本与系统版本

## 这个客户端的安全边界

了解这些边界有助于判断某个现象算不算漏洞：

- 客户端**只读写绑定的博客项目目录**下的 `src/content`、`src/config`、`public`，并调用本机 git；
  没有服务端、没有账号体系，也不往任何地方上传数据。
- 设置、壁纸与运行数据都放在客户端自己的 `data/` 目录里（安装版是安装目录下的 `data/`）。
- 「动态发布」会内嵌远端网页（Memos），底层是 Chromium 的 webview：证书不受信任时会弹窗询问，页面里的外链一律交给系统浏览器打开。
- 安装包由本仓库的 GitHub Actions 构建（`pnpm dist` → NSIS）并发布在 Releases 里，请核对下载来源。

## 响应

个人项目，尽力尽快处理。漏洞确认并修复后会在 Release 说明里注明影响范围与修复版本。
